# Analysis Report: {target_name}

**Level:** {project | api | module | function}
**Depth:** {quick | deep}
**Date:** {YYYY-MM-DD}
**Commit:** {short-hash}

---

## Codebase Metrics

> Generated by `tokei`. Scoped to the analysis target directory.

| Language | Files | Code | Comments | Blanks |
|----------|------:|-----:|---------:|-------:|
| {lang}   | {n}   | {n}  | {n}      | {n}    |
| **Total** | **{n}** | **{n}** | **{n}** | **{n}** |

---

## Overview

{Executive summary: what is being analyzed, why it matters, and the top 3 findings.
Keep this to 3-5 sentences. Lead with the most important insight.}

---

## Structure

> Include for **project** and **module** level analyses. Omit for api/function level.

{Directory layout of the analysis target. Classify directories as:
- **Core** — actively developed, business logic
- **Supporting** — config, scripts, generated code, vendored deps

Use a tree diagram. Mark entry points with arrows.}

---

## Details

{Level-specific breakdown. Apply the rules below based on the analysis level.

### Project Level
- All public interfaces (HTTP routes, RPC methods) listed in a table
- End-to-end request path from ingress to storage to egress
- Observability points: where critical logs are emitted, where metrics are captured

### API Level
- Single endpoint lifecycle from request to response
- External dependencies table:

| Dependency | Type | Direction | Notes |
|------------|------|-----------|-------|
| {name}     | RPC / DB / Config / Cache | Read / Write / Both | {details} |

### Module Level
- Business logic breakdown by domain operation
- State changes and decision trees
- Inter-module dependencies

### Function Level
- Cyclomatic complexity assessment
- Data mutation and type conversion points
- Call stack hierarchy (depth-limited per analysis depth setting)
}

---

## Data & Logic Flow

{Describe how data moves through the analyzed scope.
For quick mode: prose description with bullet points.
For deep mode: include Mermaid sequence or flowchart diagram.}

---

## Visualizations

> **Deep mode only.** Omit this section entirely in quick mode.

{Mermaid flowcharts following [flowchart-constraints.md](../references/flowchart-constraints.md).

Rules:
- Cap at 30 nodes per diagram. If the flow exceeds 30 nodes, split into sub-diagrams.
- Use subgraph blocks to group related nodes.
- Color external calls (DB, RPC, API) with `fill:#f96`.
- Label edges with the data being passed.}

```mermaid
graph TD
    %% Replace with actual analysis content
```

---

## Dependencies

{List all external systems touched by the analyzed scope.

| System | Type | Access Pattern | Config Source |
|--------|------|---------------|---------------|
| {name} | DB / Cache / RPC / MQ / Config | Read / Write / Pub / Sub | {where configured} |
}

---

## Summary

{Objective conclusions based strictly on code evidence. Do NOT hallucinate features not present in the code.

Include:
- Key architectural decisions observed
- Potential risks or complexity hotspots
- Suggestions for further analysis (if any areas warrant deeper investigation)

Keep to 3-5 bullet points.}
